# Contributing

## Setup

```bash
pnpm install
pnpm dev
```

Use focused changes and add tests for public behavior. Keep generated artifacts,
credentials, local uploads, and package tarballs out of commits.

Before requesting review, run `pnpm check` and describe any manual browser checks.
Public API changes must update the README examples and changelog.
